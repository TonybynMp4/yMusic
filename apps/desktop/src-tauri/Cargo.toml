[package]
name = "ytbm"
version = "0.1.0"
description = "A native YouTube Music player"
edition = "2021"
rust-version = "1.82"

[lib]
# Required so the same core can back the mobile targets later.
name = "ytbm_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset"] }
tauri-plugin-opener = "2"
tauri-plugin-http = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
tauri-plugin-log = "2"
libmpv2 = "6.0.0"
lofty = "0.22.2"
walkdir = "2.5.0"
sha2 = "0.10.9"
rusqlite = { version = "0.40.2", features = ["bundled"] }
thiserror = "2.0.20"
tauri-plugin-dialog = "2.7.3"

[dev-dependencies]
tauri = { version = "2", features = ["test", "protocol-asset"] }
serde_json = "1"

[target.'cfg(windows)'.dependencies]
window-vibrancy = "0.6"

[target."cfg(unix)".dependencies]
libc = "0.2.189"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
